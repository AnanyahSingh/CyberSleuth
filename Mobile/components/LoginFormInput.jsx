import { View, Text, TextInput } from "react-native";
import React, { useState } from "react";

const LoginFormInput = ({ label, placeholder, secureTextEntry, field }) => {
  

  return (
    
  );
};

export default LoginFormInput;
